{
  "name": "@spy4x/preact-cn",
  "version": "0.1.1",
  "exports": {
    ".": "./cn.ts"
  },
  "publish": {
    // This package's own `*.test.*` suites, none reachable from `exports`; see `ui/deno.json` for
    // why the glob starts with `**/`.
    "exclude": ["**/*.test.ts", "**/*.test.tsx"]
  }
}
